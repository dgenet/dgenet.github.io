quest_show = (localStorage.getItem('quest_show')) ? localStorage.getItem('quest_show') : true;
const quest_checkbox = document.getElementById('quest_check');

if (quest_show && quest_checkbox) quest_checkbox.checked = true;

if (quest_checkbox) quest_checkbox.addEventListener( 'change', function() {
    if(this.checked) {
        quest_show = true;
    } else {
        quest_show = false;
    }
    localStorage.setItem('quest_show', quest_show);

    browser.tabs.query(params)
});
